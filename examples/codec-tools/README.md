# Codec Tools

This is a source-only plugin example. Build tooling can package these files into one `.wasm` plugin bundle; the generated `.wasm` file should not be committed.

The example has one plugin with two independent capabilities:

- `base64`
- `json-xml`
