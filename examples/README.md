# RA Plugin Examples

This directory contains demo plugin source packages.

Build one into a single `.wasm` plugin package and install it through the plugin manager when you want to test it. Generated `.wasm` packages should not be committed. Demos are kept outside `../plugins/` so they do not load by default.
